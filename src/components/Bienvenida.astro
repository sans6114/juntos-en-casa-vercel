---
import { imagesCarousel } from "../information/imagenesCarousel";
import Carousel from "./shared/Carousel.astro";
import Logo from "./shared/Logo.astro";
---

<div class="relative grid-bienvenida w-full h-screen overflow-hidden bg-black">
  <!-- Texto que aparece después -->
  <div
    class="max-h-[300px] md:self-start self-center children-1"
    data-aos="fade-right"
    data-aos-delay="100"
    data-aos-duration="800"
  >
    <Logo class="w-xs md:w-xl" />
  </div>
  <div
    data-aos="fade-up"
    data-aos-duration="800"
    class="children-2 flex justify-center px-6"
  >
    <!-- <Carousel images={imagesCarousel} /> -->
    <p class="text-white text-center font-semibold text-2xl px-4">
      "Por eso les digo: dejen que el Espíritu Santo los guíe en la vida"<br />

      Gálatas 5:25
    </p>
  </div>
  <div data-aos="fade-up" data-aos-delay="200" class="children-3">
    <img
      src="/personaje/llama.svg"
      alt="Personaje"
      class="absolute h-[500px] w-[500px]"
    />
  </div>
</div>
<style>
  .grid-bienvenida {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    grid-template-rows: repeat(6, 1fr);
    gap: 8px;

    .children-1 {
      grid-row-start: 1;
      grid-row-end: 2;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .children-2 {
      grid-column-start: 1;
      grid-row-start: 2;
      grid-row-end: 5;
      align-self: center;
    }
    .children-3 {
      grid-column-start: 1;
      grid-row-start: 4;
      grid-row-end: 5;
    }
  }
  @media (min-width: 1024px) {
    .grid-bienvenida {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      grid-template-rows: repeat(5, 1fr);
      gap: 8px;

      .children-1 {
        grid-column: span 2 / span 2;
        grid-row: span 2 / span 2;
      }
      .children-2 {
        grid-column: span 2 / span 2;
        grid-row: span 2 / span 2;
        grid-column-start: 1;
        grid-row-start: 4;
      }
      .children-3 {
      }
    }
  }
  .personaje {
    z-index: 100;
    bottom: 40px;
    width: 300px;
  }
  /* Tablets grandes (1024px+) */
  @media (min-width: 1024px) {
    .personaje {
      bottom: -100px;
      right: 80px;
      width: 350px;
    }
  }
</style>
